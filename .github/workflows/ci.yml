name: CI on Self Hosted Runner

on:
  push:
    branches:
      - main

jobs:
  test:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4

      - name: Verify runner
        run: |
          echo "Runner picked up the job 🚀"
          hostname
          whoami
